/*
let markWeight = 95;
let johnWeight = 85;
let markHeight = 1.88;
let johnHeight = 1.75;
let bmiMArk = markWeight / markHeight ** 2;
let bmiJohn = johnWeight / johnHeight ** 2;
if (bmiMArk > bmiJohn)
  console.log(`Mark's BMI (${bmiMArk}) is higher than
John's BMI(${bmiJohn})`);
else
  console.log(`John's BMI(${bmiJohn}) is higher than Mark's BMI (${bmiMArk})`);
*/

/*
const averageDolphin = (97 + 112 + 101) / 3;
const averageKoalas = (109 + 95 + 106) / 3;

if (averageDolphin >= 100 && averageKoalas >= 100) {
  if (averageDolphin > averageKoalas)
    console.log(`Dolhins (${averageDolphin}) win the game`);
  else if (averageKoalas > averageDolphin)
    console.log(`Koalas (${averageKoalas}) win the game`);
  else console.log(`Its a draw`);
} else console.log(`None of the team has score greater than 100`);
*/

/*
let billValue=430;
let tip=(billValue>=50) && (billValue<=300) ? (billValue*15)/100 : (billValue*20)/100;
console.log(tip);
console.log(`The bill was ${billValue}, the tip was ${tip}, and the total value is ${billValue+tip}`);
*/
